# Code style guides

Add a focused guide only when a repository convention is important, repeated,
and not already enforced by formatting, linting, tests, `AGENTS.md`, or another
authoritative document.
